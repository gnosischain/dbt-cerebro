version: 2
models:

- name: contracts_backedfi_TSLAx_Oracle_events
  description: This model captures Oracle events related to TSLAx contracts, including transaction details, event types, and timestamps for analysis.
  columns:
  - name: block_timestamp
    description: The timestamp when the block was mined, in UTC.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block, indicating order.
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction.
    data_type: String
  - name: event_type
    description: The type of the Oracle event (e.g., 'PriceUpdate', 'Settlement').
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data.
    data_type: JSON
  - name: contract_address
    description: The address of the contract emitting the event.
    data_type: String
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: transaction_index
    description: The index of the transaction within the block.
    data_type: UInt64
  - name: sender_address
    description: Address of the transaction sender.
    data_type: String
  - name: receiver_address
    description: Address of the transaction receiver.
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bC3M_Oracle_events
  description: This model captures Oracle event logs related to BackedFi, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Log index within the block, used to order events
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction that emitted the event
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event
    data_type: String
  - name: oracle_id
    description: Identifier for the Oracle instance
    data_type: String
  - name: round_id
    description: Identifier for the specific round of the Oracle
    data_type: String
  - name: answer
    description: The answer provided by the Oracle, in wei or relevant units
    data_type: String
  - name: answered_in_round
    description: Round number in which the answer was provided
    data_type: String
  - name: created_at
    description: Timestamp when the event record was created in the system
    data_type: DateTime
  - name: updated_at
    description: Timestamp when the event record was last updated
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bCOIN_Oracle_events
  description: This model captures Oracle event logs related to backedfi bCOIN, including transaction details, block information, and event-specific data for analytics and reporting purposes.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was emitted (UTC)
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block (sequence number)
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash of the transaction associated with the event
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event
    data_type: String
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: amount_wei
    description: Amount involved in the event, in wei
    data_type: String
  - name: token_address
    description: Address of the token involved in the event
    data_type: String
  - name: sender_address
    description: Address initiating the transaction
    data_type: String
  - name: receiver_address
    description: Address receiving tokens or assets
    data_type: String
  - name: additional_data
    description: JSON containing extra event details
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bCSPX_Oracle_events
  description: This model captures Oracle events related to backedfi bCSPX, including timestamped logs and associated details.
  columns:
  - name: block_timestamp
    description: The timestamp of the block when the event was emitted.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block (sequence number).
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash of the transaction that emitted the event.
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name identifier of the event type.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data.
    data_type: JSON
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: sender_address
    description: Address of the sender involved in the event.
    data_type: String
  - name: recipient_address
    description: Address of the recipient involved in the event.
    data_type: String
  - name: amount
    description: Amount transferred or involved, in wei.
    data_type: String
  - name: gas_used
    description: Gas used for the transaction.
    data_type: UInt64
  - name: gas_price
    description: Gas price at the time of transaction, in wei.
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event.
    data_type: String
  - name: log_topics
    description: List of topics associated with the log.
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bHIGH_Oracle_events
  description: This model captures Oracle event logs related to the backedfi protocol, including transaction details, block information, and event-specific data.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded.
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction associated with the event.
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the Oracle event.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data.
    data_type: JSON
  - name: block_number
    description: Number of the block containing the event.
    data_type: UInt64
  - name: sender_address
    description: Address of the sender initiating the transaction.
    data_type: String
  - name: gas_price
    description: Gas price paid for the transaction, in wei.
    data_type: String
  - name: gas_used
    description: Gas used by the transaction.
    data_type: UInt64
  - name: value_transferred
    description: Amount transferred in the transaction, in wei.
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event.
    data_type: String
  - name: log_data
    description: Raw log data payload.
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bIB01_Oracle_events
  description: This model captures Oracle event logs related to BackedFi, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event
    data_type: String
  - name: oracle_id
    description: Identifier for the Oracle involved, if applicable
    data_type: String
  - name: request_id
    description: Unique request identifier for the Oracle request
    data_type: String
  - name: request_data
    description: Raw data payload of the Oracle request
    data_type: JSON
  - name: response_data
    description: Raw data payload of the Oracle response
    data_type: JSON
  - name: status
    description: Status of the Oracle request (e.g., 'pending', 'fulfilled', 'failed')
    data_type: String
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: transaction_index
    description: Index of the transaction within the block
    data_type: UInt64
  - name: event_data
    description: Additional event-specific data
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bIBTA_Oracle_events
  description: This model captures Oracle event logs related to BackedFi, including transaction details and event metadata.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction associated with the event
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event emitted
    data_type: String
  - name: event_data
    description: JSON containing event-specific data payload
    data_type: JSON
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: transaction_index
    description: Index of the transaction within the block
    data_type: UInt64
  - name: sender_address
    description: Address of the sender initiating the transaction
    data_type: String
  - name: gas_used
    description: Amount of gas used for the transaction
    data_type: UInt64
  - name: status
    description: Status of the transaction (e.g., success or failure)
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bMSTR_Oracle_events
  description: This model captures Oracle events related to backedfi, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: transaction_hash
    description: Unique identifier for the transaction in which the event was emitted.
    data_type: String
  - name: block_number
    description: The block height at which the event was included.
    data_type: UInt64
  - name: block_timestamp
    description: Timestamp when the block was mined, in UTC.
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_type
    description: Type of the Oracle event, indicating its purpose.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data payload.
    data_type: JSON
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  - name: updated_at
    description: Timestamp when the record was last updated.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bNVDA_Oracle_events
  description: This model processes Oracle event logs related to backedfi bNVDA contracts, capturing event details, transaction info, and block metadata for analysis.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order logs
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction
    data_type: String
  - name: event_name
    description: Name of the Oracle event emitted
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data
    data_type: JSON
  - name: contract_address
    description: Address of the Oracle contract emitting the event
    data_type: String
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: sender_address
    description: Address initiating the transaction
    data_type: String
  - name: gas_price
    description: Gas price paid for the transaction in wei
    data_type: String
  - name: gas_used
    description: Amount of gas used for the transaction
    data_type: String
  - name: status
    description: Transaction status (e.g., success, failure)
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event
    data_type: String
  - name: event_id
    description: Unique identifier for the event log
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
