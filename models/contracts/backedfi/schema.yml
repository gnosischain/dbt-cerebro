version: 2
models:

- name: contracts_backedfi_TSLAx_Oracle_events
  description: This model captures Oracle events related to TSLA from the BackedFi contracts, including timestamp, log index, and related transaction details.
  columns:
  - name: block_timestamp
    description: The timestamp of the block when the event was emitted.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block.
    data_type: UInt64
  - name: transaction_hash
    description: The hash of the transaction that emitted the event.
    data_type: String
  - name: event_type
    description: The type of Oracle event (e.g., 'Update', 'Create').
    data_type: String
  - name: oracle_value
    description: The value provided by the Oracle, in wei or relevant units.
    data_type: String
  - name: sender_address
    description: The address that sent the transaction.
    data_type: String
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: transaction_index
    description: The index of the transaction within the block.
    data_type: UInt64
  - name: event_data
    description: Additional event data in JSON format.
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bC3M_Oracle_events
  description: This model captures Oracle event logs related to BackedFi, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order logs
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction associated with the event
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event emitted
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data
    data_type: JSON
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: sender_address
    description: Address of the sender initiating the transaction
    data_type: String
  - name: amount_wei
    description: Amount involved in the event, in wei
    data_type: String
  - name: fee_gwei
    description: Transaction fee in gwei
    data_type: String
  - name: status
    description: Status of the transaction or event (e.g., success, failure)
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bCOIN_Oracle_events
  description: This model captures Oracle event logs related to backedfi bCOIN, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: block_timestamp
    description: The timestamp when the block was mined, in UTC.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block, indicating order.
    data_type: UInt64
  - name: transaction_hash
    description: The hash of the transaction associated with the event.
    data_type: String
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: contract_address
    description: The address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: The name of the Oracle event.
    data_type: String
  - name: oracle_id
    description: Identifier for the Oracle involved in the event.
    data_type: String
  - name: token_amount
    description: The amount of tokens involved, in wei.
    data_type: String
  - name: token_amount_gwei
    description: The amount of tokens involved, in gwei.
    data_type: String
  - name: event_data
    description: Additional data associated with the event, in JSON format.
    data_type: JSON
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  - name: updated_at
    description: Timestamp when the record was last updated.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bCSPX_Oracle_events
  description: This model captures Oracle events related to backedfi bCSPX, including transaction details, block information, and event-specific data.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was emitted.
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block.
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction.
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the Oracle event.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data.
    data_type: JSON
  - name: block_number
    description: Number of the block containing the event.
    data_type: UInt64
  - name: transaction_index
    description: Index of the transaction within the block.
    data_type: UInt64
  - name: sender_address
    description: Address of the sender initiating the transaction.
    data_type: String
  - name: recipient_address
    description: Address of the recipient, if applicable.
    data_type: String
  - name: amount_wei
    description: Amount involved in the event, in wei.
    data_type: String
  - name: event_signature
    description: Signature of the emitted event.
    data_type: String
  - name: status
    description: Status of the event processing.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bHIGH_Oracle_events
  description: This model captures Oracle event logs related to the backedfi high contract, including transaction details, block information, and event-specific data.
  columns:
  - name: block_timestamp
    description: The timestamp of the block when the event was recorded.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block, used to order events.
    data_type: UInt64
  - name: transaction_hash
    description: The hash of the transaction that generated the event.
    data_type: String
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: contract_address
    description: The address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: The name of the Oracle event.
    data_type: String
  - name: event_data
    description: The JSON payload containing event-specific data.
    data_type: JSON
  - name: sender_address
    description: The address initiating the transaction.
    data_type: String
  - name: gas_used
    description: The amount of gas used for the transaction, in wei.
    data_type: String
  - name: status
    description: The status of the transaction (e.g., success, failure).
    data_type: String
  - name: block_hash
    description: The hash of the block containing the event.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bIB01_Oracle_events
  description: This model captures Oracle event logs related to BackedFi BIB01 contracts, including transaction details, block information, and event-specific data for analysis and reporting.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was emitted, in UTC.
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction that emitted the event.
    data_type: String
  - name: block_number
    description: Number of the block containing the event.
    data_type: UInt64
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the Oracle event emitted.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data payload.
    data_type: JSON
  - name: sender_address
    description: Address of the sender initiating the transaction.
    data_type: String
  - name: gas_price
    description: Gas price paid for the transaction in wei.
    data_type: String
  - name: gas_used
    description: Amount of gas used by the transaction.
    data_type: UInt64
  - name: status
    description: Status of the transaction execution (e.g., success or failure).
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bIBTA_Oracle_events
  description: This model captures Oracle event logs related to backedfi BIBTA contracts, including transaction details, block information, and event-specific data.
  columns:
  - name: block_timestamp
    description: The timestamp of the block containing the event, in UTC.
    data_type: DateTime
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: log_index
    description: The index of the log within the block.
    data_type: UInt64
  - name: transaction_hash
    description: The hash of the transaction that generated the event.
    data_type: String
  - name: contract_address
    description: The address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: The name of the Oracle event.
    data_type: String
  - name: oracle_id
    description: The identifier of the Oracle involved in the event.
    data_type: String
  - name: data
    description: Event-specific data payload, in JSON format.
    data_type: JSON
  - name: sender
    description: Address of the sender initiating the event.
    data_type: String
  - name: amount
    description: The amount involved in the event, in wei.
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bMSTR_Oracle_events
  description: This model captures Oracle events related to BackedFi's bMSTR contract, including transaction details, block information, and event-specific data.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was recorded
    data_type: DateTime
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: log_index
    description: Index of the log within the block
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction that emitted the event
    data_type: String
  - name: event_name
    description: Name of the Oracle event
    data_type: String
  - name: contract_address
    description: Address of the Oracle contract emitting the event
    data_type: String
  - name: data
    description: Event-specific data payload in JSON format
    data_type: JSON
  - name: block_hash
    description: Hash of the block containing the event
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database
    data_type: DateTime
  - name: updated_at
    description: Timestamp when the record was last updated
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_backedfi_bNVDA_Oracle_events
  description: This model processes Oracle event logs related to BackedFi's NVDA contract, capturing event details and transaction metadata for analysis.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the event was emitted
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction
    data_type: String
  - name: contract_address
    description: Address of the Oracle contract emitting the event
    data_type: String
  - name: event_name
    description: Name of the event emitted, e.g., 'PriceUpdated'
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data
    data_type: JSON
  - name: sender_address
    description: Address of the transaction sender
    data_type: String
  - name: block_number
    description: Number of the block containing the event
    data_type: UInt64
  - name: transaction_index
    description: Index of the transaction within the block
    data_type: UInt64
  - name: gas_price
    description: Gas price paid for the transaction in wei
    data_type: String
  - name: gas_used
    description: Amount of gas used by the transaction
    data_type: UInt64
  - name: status
    description: Transaction status code
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - backedfi
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
