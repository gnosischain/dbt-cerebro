version: 2
models:

- name: contracts_Seer_MarketFactory_calls
  description: This model captures call data related to the Seer MarketFactory contracts, including transaction details, block information, and call specifics.
  columns:
  - name: block_timestamp
    description: The timestamp when the block was mined, in UTC.
    data_type: DateTime
  - name: transaction_hash
    description: Unique hash identifying the transaction.
    data_type: String
  - name: block_number
    description: The number of the block containing the transaction.
    data_type: UInt64
  - name: log_index
    description: The index of the log within the block.
    data_type: UInt64
  - name: caller_address
    description: Address of the caller initiating the transaction.
    data_type: String
  - name: contract_address
    description: Address of the contract being called.
    data_type: String
  - name: call_data
    description: Encoded call data payload.
    data_type: String
  - name: output_json_type
    description: Indicates if the output is in JSON format; boolean flag.
    data_type: String
  - name: output_data
    description: The output data from the call, in JSON format.
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - seermarketfactory
    - calls
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_Seer_MarketFactory_events
  description: This model captures event logs related to the Seer MarketFactory, including transaction details, block information, and event-specific data, used for analytics and reporting.
  columns:
  - name: block_timestamp
    description: Timestamp of the block containing the event, in UTC.
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash identifying the transaction.
    data_type: String
  - name: block_number
    description: Number of the block containing the event.
    data_type: UInt64
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the event emitted.
    data_type: String
  - name: event_data
    description: JSON object containing event-specific data.
    data_type: JSON
  - name: sender_address
    description: Address of the sender involved in the event.
    data_type: String
  - name: receiver_address
    description: Address of the receiver involved in the event.
    data_type: String
  - name: amount
    description: Amount transferred in wei.
    data_type: UInt64
  - name: token_id
    description: Identifier for the token involved.
    data_type: String
  - name: block_hash
    description: Hash of the block containing the event.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - seermarketfactory
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
- name: contracts_Seer_Wrapped1155Factory_calls
  description: This model captures call data for the Seer Wrapped1155Factory contract, including transaction details, block info, and call specifics.
  columns:
  - name: block_timestamp
    description: The timestamp when the block was mined, in UTC.
    data_type: DateTime
  - name: transaction_hash
    description: Unique hash identifying the transaction.
    data_type: String
  - name: block_number
    description: The number of the block containing the transaction.
    data_type: UInt64
  - name: log_index
    description: Index of the log within the block.
    data_type: UInt64
  - name: caller_address
    description: Address initiating the call.
    data_type: String
  - name: contract_address
    description: Address of the contract being called.
    data_type: String
  - name: function_name
    description: Name of the function invoked in the call.
    data_type: String
  - name: call_data
    description: Encoded call data payload.
    data_type: String
  - name: output_json_type
    description: Indicates if the output is in JSON format.
    data_type: UInt64
  - name: output_data
    description: The output data from the call, in JSON or raw format.
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - seerpm
    - wrapped1155factory
    - calls
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_Seer_Wrapped1155Factory_events
  description: Event logs emitted by the Wrapped1155Factory contract, capturing creation, transfer, and approval events related to ERC-1155 tokens.
  columns:
  - name: block_number
    description: The block number at which the event was included.
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction that emitted the event.
    data_type: String
  - name: log_index
    description: Index of the log within the block.
    data_type: UInt64
  - name: block_timestamp
    description: Timestamp when the block was mined.
    data_type: DateTime
  - name: event_type
    description: Type of the event (e.g., 'Transfer', 'Approval', 'Create').
    data_type: String
  - name: contract_address
    description: Address of the Wrapped1155Factory contract emitting the event.
    data_type: String
  - name: from_address
    description: Address of the sender (for transfer and approval events).
    data_type: String
  - name: to_address
    description: Address of the receiver (for transfer and approval events).
    data_type: String
  - name: token_id
    description: Identifier of the ERC-1155 token involved.
    data_type: String
  - name: amount
    description: Quantity of tokens transferred or approved.
    data_type: String
  - name: operator
    description: Address authorized to operate on the tokens (for approvals).
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the database.
    data_type: DateTime
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - seerpm
    - wrapped1155factory
    - events
  meta:
    owner: analytics-team
    authoritative: false
    generated_by: schema-writer
