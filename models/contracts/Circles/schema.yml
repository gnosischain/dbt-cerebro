version: 2
models:

- name: contracts_circles_v1_Hub_events
  description: This model captures event data related to contracts circles, including timestamps, identifiers, and related metadata for analysis and reporting.
  columns:
  - name: block_timestamp
    description: The timestamp when the block containing the event was mined.
    data_type: DateTime
  - name: log_index
    description: The index of the log within the block.
    data_type: UInt64
  - name: block_number
    description: The number of the block containing the event.
    data_type: UInt64
  - name: transaction_hash
    description: The hash of the transaction that generated the event.
    data_type: String
  - name: contract_address
    description: The address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: The name of the event emitted.
    data_type: String
  - name: circle_id
    description: Unique identifier for the circle related to the event.
    data_type: String
  - name: circle_name
    description: Human-readable name of the circle.
    data_type: String
  - name: sender_address
    description: Address of the sender initiating the event.
    data_type: String
  - name: recipient_address
    description: Address of the recipient involved in the event.
    data_type: String
  - name: event_value
    description: The value associated with the event, in wei.
    data_type: String
  - name: event_metadata
    description: Additional metadata for the event in JSON format.
    data_type: JSON
  config:
    access: public
    tags:
    - production
    - contracts
    - circles
    - events
    materialized: incremental
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_circles_v2_Hub_events
  description: This model processes hub events related to contracts and circles, capturing event details, timestamps, and identifiers for business analysis.
  columns:
  - name: block_timestamp
    description: Timestamp of the event in UTC
    data_type: DateTime
  - name: log_index
    description: Index of the log within the block
    data_type: UInt64
  - name: transaction_hash
    description: Unique hash of the transaction
    data_type: String
  - name: event_name
    description: Name of the event emitted
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event
    data_type: String
  - name: circle_id
    description: Identifier for the circle involved
    data_type: String
  - name: contract_id
    description: Identifier for the contract involved
    data_type: String
  - name: event_data
    description: JSON containing event-specific data
    data_type: JSON
  config:
    access: public
    tags:
    - production
    - contracts
    - circles
    - events
    materialized: incremental
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_circles_v2_NameRegistry_events
  description: This model captures NameRegistry events related to contracts circles v2, including event details, timestamps, and transaction info.
  columns:
  - name: event_id
    description: Unique identifier for each event.
    data_type: String
  - name: block_number
    description: Blockchain block number where the event was recorded.
    data_type: UInt64
  - name: block_timestamp
    description: Timestamp of the block containing the event.
    data_type: DateTime64(0, 'UTC')
  - name: log_index
    description: Index of the log within the block.
    data_type: UInt64
  - name: transaction_hash
    description: Hash of the transaction that emitted the event.
    data_type: String
  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the event emitted.
    data_type: String
  - name: owner_address
    description: Address of the owner involved in the event.
    data_type: String
  - name: name
    description: Name associated with the registry event.
    data_type: String
  - name: registration_time
    description: Time when the registration occurred.
    data_type: DateTime64(0, 'UTC')
  - name: metadata
    description: Additional metadata in JSON format.
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - production
    - contracts
    - circles
    - events
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_circles_v2_CirclesBackingFactory_events
  description: This table captures event data emitted by the CirclesBackingFactory contract, including transaction details, block info, and event-specific parameters.
  columns:
  - name: id
    description: Unique identifier for each event record, generated as a primary key.
    data_type: String
  - name: block_number
    description: Block number in which the event was included.
    data_type: UInt64
  - name: block_timestamp
    description: Timestamp when the block was mined, in UTC.
    data_type: DateTime64(0, 'UTC')
  - name: transaction_hash
    description: Hash of the transaction that emitted the event.
    data_type: String
  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64
  - name: contract_address
    description: Address of the CirclesBackingFactory contract emitting the event.
    data_type: String
  - name: event_name
    description: Name of the event emitted, e.g., 'BackingCreated', 'BackingUpdated'.
    data_type: String
  - name: backing_id
    description: Unique identifier for the backing instance.
    data_type: String
  - name: owner
    description: Address of the owner associated with the backing.
    data_type: String
  - name: amount_wei
    description: Amount involved in the event, denominated in wei.
    data_type: String
  - name: backing_type
    description: Type of backing, e.g., 'collateral', 'liquidity'.
    data_type: String
  - name: created_at
    description: Timestamp when the record was created in the system.
    data_type: DateTime64(0, 'UTC')
  config:
    access: public
    tags:
    - production
    - contracts
    - circles
    - events
    materialized: incremental
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
