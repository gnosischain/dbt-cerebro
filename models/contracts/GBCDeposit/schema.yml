version: 2
models:

- name: contracts_GBCDeposit_calls
  description: The `contracts_GBCDeposit_calls` model captures and decodes call data related to GBCDeposit contract interactions, facilitating analysis of contract activity over time.
  columns:

  - name: transaction_hash
    description: Unique identifier of the transaction.
    data_type: String

  - name: block_number
    description: Number of the block containing the transaction.
    data_type: UInt64

  - name: block_timestamp
    description: Timestamp when the block was mined.
    data_type: DateTime64(0, 'UTC')

  - name: sender
    description: Address of the sender initiating the call.
    data_type: String

  - name: receiver
    description: Address of the receiver of the deposit.
    data_type: String

  - name: amount
    description: Deposit amount in wei.
    data_type: UInt64

  - name: status
    description: Status of the call (e.g., success, failed).
    data_type: String
  meta:
    owner: analytics_team
    authoritative: false
    full_refresh:
      start_date: "2021-12-01"
      batch_months: 6
    generated_by: dbt-schema-gen
    _generated_at: '2025-12-12T23:06:58.422055'
    _generated_fields:
    - description
    - config.tags
    - config.materialized

- name: contracts_GBCDeposit_events
  description: The contracts_GBCDeposit_events model captures and processes deposit event logs related to the GBC Deposit contract, providing a detailed record of deposit activities over time for analytics and reporting purposes.
  columns:

  - name: transaction_hash
    description: Unique identifier of the transaction in which the event occurred.
    data_type: String

  - name: block_number
    description: Blockchain block number where the event was recorded.
    data_type: UInt64

  - name: block_timestamp
    description: Timestamp of the block containing the event, in UTC.
    data_type: DateTime64(0, 'UTC')

  - name: log_index
    description: Index of the log within the block, used to order events.
    data_type: UInt64

  - name: contract_address
    description: Address of the contract emitting the event.
    data_type: String

  - name: depositor_address
    description: Address of the account making the deposit.
    data_type: String

  - name: deposit_amount
    description: Amount deposited, in wei.
    data_type: String

  - name: deposit_token
    description: Token type of the deposit.
    data_type: String

  - name: deposit_timestamp
    description: Timestamp when the deposit was made, in UTC.
    data_type: DateTime64(0, 'UTC')

  - name: event_type
    description: Type of event, e.g., 'Deposit'.
    data_type: String

  - name: transaction_fee
    description: Fee paid for the transaction, in wei.
    data_type: String

  - name: status
    description: Status of the deposit event, e.g., 'success' or 'failed'.
    data_type: String
  meta:
    owner: analytics_team
    authoritative: false
    full_refresh:
      start_date: "2021-12-01"
      batch_months: 6
    generated_by: dbt-schema-gen
    _generated_at: '2025-12-12T23:06:58.422113'
    _generated_fields:
    - description
    - config.tags
    - config.materialized