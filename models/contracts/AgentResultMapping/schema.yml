version: 2
models:

- name: contracts_AgentResultMapping_calls
  description: This model processes call data related to agent result mappings, including transaction details and contract interactions.
  columns:
  - name: block_timestamp
    description: Timestamp of the block when the call was made
    data_type: DateTime
  - name: transaction_hash
    description: Unique hash identifying the transaction
    data_type: String
  - name: contract_address
    description: Address of the contract involved in the call
    data_type: String
  - name: caller_address
    description: Address initiating the call
    data_type: String
  - name: call_data
    description: Encoded data payload of the call
    data_type: String
  - name: call_value
    description: Value transferred in wei during the call
    data_type: UInt64
  - name: call_type
    description: Type of call (e.g., 'call', 'delegatecall')
    data_type: String
  - name: output_json_type
    description: Indicates if output is in JSON format
    data_type: UInt64
  - name: output_data
    description: Output data from the call
    data_type: String
  config:
    access: public
    materialized: incremental
    tags:
    - agent
    - calls
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
- name: contracts_AgentResultMapping_events
  description: This model processes agent result mapping events, capturing details about contract interactions, timestamps, and related identifiers for analysis.
  columns:
  - name: block_timestamp
    description: Timestamp of the block in UTC when the event was recorded
    data_type: DateTime
  - name: log_index
    description: Log index within the block, used to order events
    data_type: UInt64
  - name: contract_address
    description: Address of the contract involved in the event
    data_type: String
  - name: agent_result_mapping_id
    description: Unique identifier for the agent result mapping event
    data_type: String
  - name: agent_result_id
    description: Identifier linking to the agent result
    data_type: String
  - name: agent_id
    description: Identifier of the agent involved
    data_type: String
  - name: result_status
    description: Status of the result, e.g., 'success', 'failure'
    data_type: String
  - name: result_value
    description: Numeric value associated with the result, in wei
    data_type: UInt64
  - name: additional_data
    description: Extra data in JSON format related to the event
    data_type: JSON
  config:
    access: public
    materialized: incremental
    tags:
    - agent_result_mapping
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
