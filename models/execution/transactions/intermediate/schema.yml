version: 2
models:

- name: int_execution_transactions_info_daily
  description: Daily aggregated transaction data including counts, values, and median metrics for execution transactions.
  columns:
  - name: date
    description: Date representing the start of the day for aggregated data (UTC).
    data_type: DateTime
  - name: transaction_type
    description: Type of transaction (e.g., transfer, contract call).
    data_type: String
  - name: success
    description: Transaction success status (1 = success, 0 = failure).
    data_type: UInt8
  - name: n_txs
    description: Total number of transactions for the given day, type, and success status.
    data_type: UInt64
  - name: xdai_value
    description: Sum of transaction values in xDai units (wei divided by 10^18).
    data_type: Float64
  - name: xdai_value_avg
    description: Average transaction value in xDai units.
    data_type: Float64
  - name: xdai_value_median
    description: Median transaction value in xDai units.
    data_type: Float64
  - name: gas_used
    description: Total gas used in transactions, in Gwei.
    data_type: Float64
  - name: gas_price_avg
    description: Average gas price in Gwei.
    data_type: Int32
  - name: gas_price_median
    description: Median gas price in Gwei.
    data_type: Int32
  config:
    access: public
    tags:
    - production
    - execution
    - transactions
    materialized: incremental
  meta:
    owner: analytics_team
    authoritative: false
    generated_by: schema-writer
