# python
**/__pycache__

# mac
.DS_Store

# environments
.env
**/.env
.user.yml
.venv/

# vscode config
.vscode

# macos metadata files
**/._*
_under_construction/*
grafana/*


# dbt stuff
target
/logs/
dbt_packages
package-lock.yml
edr_target/
reports/

**/.sqlxxx

# Full refresh state file (auto-generated)
scripts/full_refresh/.refresh_state.json

# prod-state
prod-state/
# Refresh-runner state (auto-generated; identity-keyed state lives in target/refresh_state/)
.gap_refresh_state.json
/.recover_state/

# Ad-hoc data exports at repo root (seeds/ CSVs are tracked; these are not)
/gpay_gno_balances_*.csv
/gnosis_month_active_users_*.csv

# Claude Code personal/per-machine files (settings.json IS tracked — team hooks;
# launch.json points at machine-local sibling repos — delete this line to share it)
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/
