# Generate dbt schema.yml

Follow the vendor-neutral workflow in [docs/workflows/generate-schema.md](../../docs/workflows/generate-schema.md)
with target: $ARGUMENTS (a folder, model name(s), or the current context)
