# Model review (revisit 2026-06-21): execution/ubo

Re-verification of baseline `docs/model_review/execution-ubo.md` (dated 2026-06-11), 18 cases re-checked over 3 rounds: 3 RESOLVED (Swapr decrease-orphan, mart presence/freshness, sDAI/Curve staleness gap), 4 CHANGED/downgraded (append-runbook, aGnosDAI one-pass overstatement, Balancer venue grain, venue_breakdown governance), and 11 STILL CONFIRMED (2 high latent correctness defects on the resolved-mart anti-join and cumsum FINAL asymmetry remain unfixed). No incident attributions warranted.

## Status summary

| case_id | P0 | claim (short) | orig sev | status | new sev | confidence | incident | rounds |
|---|---|---|---|---|---|---|---|---|
| EXECUTIONUBO-C01 | — | Resolved-mart strip anti-join has no `join_use_nulls=1` hook; works only because `container_address` is Nullable | high | CONFIRMED | high | high | none | 3 |
| EXECUTIONUBO-C02 | — | Three cumsum models read `{{ this }}` without FINAL in regular prev_balances (batch path uses FINAL); unmerged boundary dups inflate cumsum | high | CONFIRMED | high | high | none | 3 |
| EXECUTIONUBO-C03 | — | Resolved mart lacks `unique_combination_of_columns` test at redistribution grain | medium | CONFIRMED | medium | high | none | 3 |
| EXECUTIONUBO-C04 | — | Five intermediate claims models lack uniqueness tests (balancer/uniswap/swapr/curve/sdai) | medium | CONFIRMED | medium | high | none | 3 |
| EXECUTIONUBO-C05 | — | Strategy flips to `append` under batch vars; re-run appends dup partitions, no in-model runbook | medium | CHANGED | low | high | none | 3 |
| EXECUTIONUBO-C06 | — | `fct_ubo_known_containers_daily` pairs `materialized='table'` with `ReplacingMergeTree()` (dedup never exercised) | low | CONFIRMED | low | high | none | 3 |
| EXECUTIONUBO-C07 | — | Swapr decrease-only tokenIds dropped (no IncreaseLiquidity → no pool map) | low | RESOLVED | resolved | high | none | 3 |
| EXECUTIONUBO-C08 | — | One-pass unroll: aGnosDAI attributed to aToken, overstating `pct_known_container` | high | CHANGED | medium | high | none | 3 |
| EXECUTIONUBO-C09 | — | Uniswap/Swapr Track B argMax current-owner lookahead bias, undocumented | medium | CONFIRMED | medium | medium | none | 3 |
| EXECUTIONUBO-C10 | — | Balancer single hardcoded Vault vs per-pool Uni/Swapr; grain undocumented in venue view | medium | CHANGED | low | high | none | 3 |
| EXECUTIONUBO-C11 | — | venue_breakdown (fct+api) absent from `tokens/marts/schema.yml`; governance gap | medium | CHANGED | low | high | none | 3 |
| EXECUTIONUBO-C12 | — | Coverage/venue marts may never have built (Code 60) — confirm presence/freshness | medium | RESOLVED | resolved | high | none | 3 |
| EXECUTIONUBO-C13 | — | Pipeline scoped to `tokens_whitelist`; unlisted-token caveat implicit | low | CONFIRMED | low | medium | none | 3 |
| EXECUTIONUBO-C14 | — | Balancer Vault / Curve 3pool AMM addresses hardcoded, not seeded | low | CONFIRMED | low | high | none | 3 |
| EXECUTIONUBO-C15 | — | Row counts: claims vs resolved; net redistribution delta (data note) | low | CONFIRMED | low | high | none | 3 |
| EXECUTIONUBO-C16 | — | Second-level resolution verified correct (sDAI-only, 0 leak) | low | CONFIRMED | low | high | none | 3 |
| EXECUTIONUBO-C17 | — | sDAI/Curve 2-day staleness gap vs incremental protocols | medium | RESOLVED | resolved | high | none | 3 |
| EXECUTIONUBO-C18 | — | sDAI/EURe migration handled via whitelist date-range joins (data note) | low | CONFIRMED | low | high | none | 3 |

Rollup (final round): CONFIRMED 11, RESOLVED 3, CHANGED 4, UNVERIFIABLE 0, NEW 0, UNRESOLVED 0.

## Delta vs baseline

### RESOLVED (3)
- **EXECUTIONUBO-C07** — Swapr decrease-only tokenId drop has **zero blast radius**. `contracts_Swapr_v3_NonfungiblePositionManager_events` has `3,392` distinct `DecreaseLiquidity` tokenIds and `0` of them lack a prior `IncreaseLiquidity` (0 orphans → 0 value dropped). The pool map covers 100% of decreases. (Round 3 walked this back to UNVERIFIABLE on a code-only re-read; the orchestrator rejected the walk-back since the magnitude was measured at 0 in round 2.) No incident.
- **EXECUTIONUBO-C12** — Both marts exist, are populated and fresh; the Code-60 never-built concern is gone. `fct_execution_tokens_ubo_coverage_latest` = `25` rows, `fct_execution_tokens_ubo_venue_breakdown_latest` = `80` rows; api views `as_of_date` = `2026-06-20`. Coverage percent columns reconcile to ~`100` per token (COW `29.87 + 27.36 + 17.53 + 25.24 = 100.0`). No incident.
- **EXECUTIONUBO-C17** — The 2-day table-vs-incremental staleness gap is closed. All 7 protocols (Aave V3, Balancer V2, Curve 3pool, SparkLend, Swapr V3, Uniswap V3, sDAI) share `max(date)` = `2026-06-20` in `fct_ubo_supply_claims_daily` (baseline: incremental `2026-06-09` vs sDAI/Curve `2026-06-07`). Refresh-cadence alignment, not an incident-A wipe.

### CHANGED (4)
- **EXECUTIONUBO-C05** (medium → low) — `gap_window_refresh.py` (`scripts/refresh/gap_window_refresh.py` L205-223) runs `ALTER TABLE ... DROP PARTITION` before each append/vars re-run, compensating the dup-bloat. Confirmed the drop list covers **all four** append-mode UBO models (3 cumsum intermediates + resolved mart, all `materialized='incremental'` + `partition_by='toStartOfMonth(date)'`); curve/sdai are table-materialized, skipped, and never flip to append. Still no `OPTIMIZE TABLE FINAL` or in-model runbook comment.
- **EXECUTIONUBO-C08** (high → medium) — The aGnosDAI one-pass overstatement is now **~0%**. On `2026-06-18`, `fct_ubo_supply_claims_resolved_daily` protocol `sDAI` total `balance_usd` = `70,491,440.68`; `balance_usd` where `ubo_address = 0x7a5c3860a77a8dc1b225bd46d0fb2ac1c6d191bc` (aGnosDAI) = `NULL`/0 rows — aGnosDAI is no longer a terminal ubo, fully redistributed to Aave lenders. EURe (total `22,306,439.13`) carries no aGnosDAI layer either. Residual is documentation-only: the coverage view lacks an explicit one-pass-ceiling caveat.
- **EXECUTIONUBO-C10** (medium → low) — The baseline premise (view exposes per-pool breakdown for Uni/Swapr but collapses Balancer) is **false at the view level**. `fct_execution_tokens_ubo_venue_breakdown_latest` groups by `protocol` (SQL L51-63, `venue=p.protocol`) with **no** `container_address`/`pool_address` column for any venue; grep confirms no `tokens/marts` model SELECTs `int_ubo_claims_*`. The Balancer single-Vault vs per-pool asymmetry is internal to the intermediates only — a documentation note.
- **EXECUTIONUBO-C11** (medium → low) — Scope corrected: only `venue_breakdown` (fct+api) is absent from `models/execution/tokens/marts/schema.yml`; coverage (L844/L889) and top_holders (L681/L723/L784) **are** documented (baseline "all three absent" was wrong). `check_api_tags.py` rule #4 (L82-88) **would** flag the missing columns, but `scripts/checks/check_api_tags.allow` explicitly lists `api_execution_tokens_ubo_venue_breakdown_latest::columns_missing` and `::no_as_of_date` — a single tracked doc-exemption, not a hard CI failure.

### STILL CONFIRMED (11)
- **EXECUTIONUBO-C01** (high) — Resolved-mart strip anti-join `LEFT JOIN known_containers kc ... WHERE kc.container_address IS NULL` (`fct_ubo_supply_claims_resolved_daily.sql` clean CTE L27-34) still has **no** `SET join_use_nulls=1` pre_hook. `describe_table` confirms `container_address` is `Nullable(String)`. The only thing keeping it Nullable is config L10 `allow_nullable_key=1` + no non-null cast; `order_by`/`unique_key` (L7-8) both include `container_address`. Live strip-leak = `0` (not biting today), so high rests on the one-line-config-change blast-radius precondition.
- **EXECUTIONUBO-C02** (high) — Regular (non-batch) `prev_balances` CTE reads `FROM {{ this }}` **without** FINAL in all three cumsum models (balancer L78, uniswap L280, swapr L273) while the batch path uses FINAL (L60/L261/L254). Live unmerged boundary dups exist: balancer `15,956` (date≥`2026-06-15`, `101,047` rows vs `85,091` unique grain), uniswap `309`, swapr `11` (all FINAL = `0`). Real latent correctness defect; the orchestrator downgraded the "live corruption" framing to a near-miss after a read-site probe at the absolute boundary date (`20634`) showed `5,319` rows = `5,319` unique grain = `0` dups, but the FINAL asymmetry will double the cumsum starting balance whenever the single read-site date has an unmerged dup. Fix: add FINAL to the regular `prev_balances` read.
- **EXECUTIONUBO-C03** (medium) — Resolved mart still lacks `dbt_utils.unique_combination_of_columns`; parent `fct_ubo_supply_claims_daily` has it (schema.yml L110). Live FINAL grain dups = `0` (pure coverage gap). Remediation nuance: a default non-FINAL dbt test would false-fail on the C02 boundary dups; key the test against a FINAL subquery or a lookback where-clause.
- **EXECUTIONUBO-C04** (medium) — Five intermediates (balancer/uniswap/swapr/curve/sdai) still lack uniqueness tests; only aave (L446) and second_level (L53) carry them. All FINAL grain dups = `0`. Materialization split: a default non-FINAL test would false-fail on the 3 incremental cumsums (balancer `15,956` / uniswap `309` / swapr `11` pre-FINAL) but pass on the 2 table-materialized models (curve/sdai = `0` non-FINAL). curve/sdai can take a plain test; balancer/uniswap/swapr need a FINAL-qualified test.
- **EXECUTIONUBO-C06** (low) — `fct_ubo_known_containers_daily.sql` still pairs `materialized='table'` (L3) with `engine='ReplacingMergeTree()'` (L4); body is `SELECT DISTINCT` (L23). Full CTAS + DISTINCT means the replacing-merge dedup never fires; engine choice is cosmetic. Table live: `69,726` rows, max date `2026-06-18`.
- **EXECUTIONUBO-C09** (medium) — argMax current-owner lookahead unchanged (uniswap L43-49 / comment L116-122; swapr L39-45), still undocumented in `intermediate/schema.yml` ubo_address descriptions (uniswap L223-227, swapr L318-322). Transferred-position share by tokenId count: Uniswap `94/5,589` = `1.68%`, Swapr `675/4,804` = `14.05%`. Value-weighting not re-run in round 3; medium holds since the count-share bound was never contradicted.
- **EXECUTIONUBO-C13** (low) — Whitelist `INNER JOIN tokens_whitelist` scoping confirmed in every protocol branch of `fct_ubo_supply_claims_daily`; consumer-facing schema description (L31-53) still carries no explicit unlisted-token-appears-as-raw-container caveat. Correct-by-design, doc gap only.
- **EXECUTIONUBO-C14** (low) — Exact-hex grep: Balancer Vault `0xba12222222228d8ba445958a75a0704d566bf2c8` and Curve 3pool AMM `0x7f90122bf0700f9e7e1f688fe926940e8839f353` are absent from all `seeds/` files; hardcoded only in `int_ubo_claims_balancer_v2_daily.sql` L173 and `int_ubo_claims_curve_daily.sql` L23 (+ schema descriptions). Seed registries exist (`contracts_factory_registry.csv`, `contracts_whitelist.csv`) but do not include these two container contracts.
- **EXECUTIONUBO-C15** (low, data note) — `fct_ubo_supply_claims_daily` FINAL = `53,409,975`; `fct_ubo_supply_claims_resolved_daily` FINAL = `54,177,969`; delta = `+767,994` (baseline `+753,637`). Same sign/magnitude; the surplus is the redistribution fan-out (`7,785` second-level container rows across `990` stripped `(date,container,token)` groups replaced by underlying sub-holders), grows proportionally with supply.
- **EXECUTIONUBO-C16** (low, verified-correct) — `int_ubo_second_level_daily` FINAL = `7,785` rows / `11` distinct ubo_address / 100% sDAI (Balancer + Aave = `0`); `990` distinct stripped groups; grain dups = `0`; token-paired leak anti-join into resolved mart = `0`. No double-counting (baseline `7,664`; +121 over 10 days, normal accrual).
- **EXECUTIONUBO-C18** (low, verified-correct) — Swapr EURe migration gapless. `int_ubo_claims_swapr_v3_daily` FINAL: EURe V1 `0xcb444e90...` = `406` rows (dates `19750`-`19959`, through 2024-08-24); EURe V2 `0x420ca0f9...` = `1,350` rows (dates `19960`-`20634`, from 2024-08-25). Cutover exact and contiguous, both collapse to `symbol='EURe'`.

### NEW (0)
None.

### UNVERIFIABLE / UNRESOLVED (0)
None. (C07 had a round-3 UNVERIFIABLE regression that the orchestrator rejected in favor of the round-2 RESOLVED/zero-blast-radius result.)

## Evidence appendix

**C01** — `describe_table fct_ubo_supply_claims_resolved_daily`: `container_address Nullable(String)`, `ubo_address Nullable(String)`. SQL clean CTE L27-34: `LEFT JOIN known_containers kc ... WHERE kc.container_address IS NULL`; config L7-8 `order_by`/`unique_key` include `container_address`; L10 `allow_nullable_key=1`; no `SET join_use_nulls=1` pre_hook. Paired strip leak:
`SELECT count(*) FROM fct_ubo_supply_claims_resolved_daily r FINAL INNER JOIN (SELECT DISTINCT date,container_address,token_address FROM fct_ubo_known_containers_daily) kc ON r.date=kc.date AND r.ubo_address=kc.container_address AND r.container_address=kc.token_address` → `0`.

**C02** — Code: balancer L78 / uniswap L280 / swapr L273 read `FROM {{ this }}` (no FINAL) in regular `prev_balances`; batch path L60/L261/L254 use FINAL. `SELECT count(*), uniqExact(date,container_address,ubo_address,token_address) FROM int_ubo_claims_balancer_v2_daily WHERE date>=toDate('2026-06-15')` → `101,047` / `85,091` (dups `15,956`); FINAL = `85,091`. uniswap dups `309`, swapr dups `11` (both FINAL = `0`). Read-site boundary probe (date `20634`): `5,319` rows = `5,319` unique = `0` dups.

**C03** — `marts/schema.yml`: resolved entry L128-214 has only `elementary.schema_changes` (L199) + 2 `expression_is_true` (L203/L209), no `unique_combination_of_columns`; parent has it L110-112. `SELECT count(*)-uniqExact(date,protocol,container_address,ubo_address,token_address) FROM fct_ubo_supply_claims_resolved_daily FINAL` → `0`.

**C04** — `intermediate/schema.yml`: balancer(L62-159)/uniswap(L161-265)/swapr(L267-360)/curve(L464-541)/sdai(L543-620) carry only schema_changes + lowercase + non_negative tests; aave (L446) and second_level (L53) carry uniqueness. Per-model `count(*)-uniqExact(date,container_address,ubo_address,token_address) FINAL` = `0` for all five. Non-FINAL (date≥`2026-06-15`): uniswap `309`, swapr `11`, sdai `0`, curve `0`.

**C05** — Code L4 of balancer/uniswap/swapr + resolved mart: `incremental_strategy=('append' if (var('start_month') or var('incremental_end_date')) else 'delete+insert')`. `scripts/refresh/gap_window_refresh.py`: L179-180 skips view/table mats; L184-190 routes conditional-strategy models to `drop+vars`; L212-223 `ALTER TABLE ... DROP PARTITION` keyed on `toStartOfMonth` ids matching the model `partition_by`. All four append-mode UBO models included.

**C06** — `fct_ubo_known_containers_daily.sql` L3 `materialized='table'`, L4 `engine='ReplacingMergeTree()'`, L23 `SELECT DISTINCT date,container_address,token_address`. `SELECT count(*), max(date) FROM fct_ubo_known_containers_daily FINAL` → `69,726` / `2026-06-18`.

**C07** — `WITH inc AS (...IncreaseLiquidity...), dec AS (...DecreaseLiquidity...) SELECT count(distinct tid) FROM dec` → `3,392`; `... WHERE tid NOT IN (SELECT tid FROM inc)` → `0` orphans → `0` value dropped (`contracts_Swapr_v3_NonfungiblePositionManager_events`).

**C08** — `SELECT sum(balance_usd), sumIf(balance_usd, ubo_address='0x7a5c3860a77a8dc1b225bd46d0fb2ac1c6d191bc') FROM fct_ubo_supply_claims_resolved_daily FINAL WHERE protocol='sDAI' AND date=toDate('2026-06-18')` → `70,491,440.68` / `NULL` (0 rows). EURe total `22,306,439.13`, aGnosDAI rows `0`. Single unroll pass: resolved mart = one clean UNION ALL redistributed (no recursion); sdai L67-69 comment notes aGnosDAI address.

**C09** — Code: uniswap L43-49 `argMax(transfer_to,(block_timestamp,log_index))`, swapr L39-45 same. Transferred share (tokenIds with ≥2 distinct non-zero Transfer `to` owners): Uniswap `94/5,589` = `1.68%`, Swapr `675/4,804` = `14.05%`. schema.yml ubo_address descriptions (uniswap L223-227, swapr L318-322) carry no lookahead caveat.

**C10** — `fct_execution_tokens_ubo_venue_breakdown_latest` SQL L51-63: `venue=p.protocol`, no container/pool column. `SELECT venue, count(*), uniqExact(token_address) ... GROUP BY venue` → 1 row per (token, protocol) for Aave V3, Balancer V2, Curve 3pool, SparkLend, Swapr V3, Uniswap V3, direct, sDAI. `int_ubo_claims_balancer_v2_daily` L173 hardcodes single Vault, no pool_address. grep `int_ubo_claims` in `tokens/marts` → empty.

**C11** — grep `tokens/marts/schema.yml`: coverage L844/L889, top_holders L681/L723/L784 present; venue_breakdown (fct+api) absent. `api_execution_tokens_ubo_venue_breakdown_latest` L4 tags `production`, `tier1`, `api:tokens_ubo_venue_breakdown`, `granularity:latest`. `check_api_tags.py` rule #4 L82-88 + `check_api_tags.allow` lists `api_execution_tokens_ubo_venue_breakdown_latest::columns_missing` and `::no_as_of_date`.

**C12** — `SELECT count(*) FROM fct_execution_tokens_ubo_venue_breakdown_latest` → `80`; `... ubo_coverage_latest` → `25`; api `max(as_of_date)` = `2026-06-20`; driving upstream `fct_ubo_known_containers_daily` max date `2026-06-18`. Coverage pct columns percent-scaled (0-100), reconcile to ~`100` per token.

**C13** — `fct_ubo_supply_claims_daily.sql` = UNION ALL of `int_ubo_claims_*` (aave/balancer/uniswap/swapr/curve/sdai), each `INNER JOIN tokens_whitelist` on symbol + date-range (e.g. balancer L31-34, uniswap L208-211). mart schema.yml description L31-53 has no unlisted-token caveat.

**C14** — Exact-hex grep across `seeds/*.csv`: no match for `0xba12222222228d8ba445958a75a0704d566bf2c8` or `0x7f90122bf0700f9e7e1f688fe926940e8839f353`. Hardcoded in `int_ubo_claims_balancer_v2_daily.sql` L173, `int_ubo_claims_curve_daily.sql` L23.

**C15** — `SELECT count(*) FROM fct_ubo_supply_claims_daily FINAL` → `53,409,975`; `... resolved_daily FINAL` → `54,177,969`; delta `+767,994`. `int_ubo_second_level_daily` = `7,785` rows / `990` stripped groups / `11` ubo.

**C16** — `int_ubo_second_level_daily FINAL`: `7,785` rows, `11` ubo, `990` groups, 100% sDAI (Balancer/Aave `0`); grain dups `0`. Token-paired leak: `SELECT count(*) FROM fct_ubo_supply_claims_resolved_daily r FINAL INNER JOIN (SELECT DISTINCT date,container_address,token_address,ubo_address FROM int_ubo_second_level_daily FINAL) s ON r.date=s.date AND r.ubo_address=s.container_address AND r.container_address=s.token_address` → `0`.

**C17** — `SELECT protocol, max(date) FROM fct_ubo_supply_claims_daily FINAL WHERE date>=toDate('2026-05-01') GROUP BY protocol` → all 7 protocols `20622` (`2026-06-20`).

**C18** — `int_ubo_claims_swapr_v3_daily FINAL` EURe split: V1 `0xcb444e90d8198415266c6a2724b7900fb12fc56e` = `406` rows (`19750`-`19959`); V2 `0x420ca0f9b9b604ce0fd9c18ef134c705e5fa3430` = `1,350` rows (`19960`-`20634`). No gap across cutover `19959`→`19960`.

## Review log (>=3 rounds per case)

- **C01** — R1 CONFIRMED (code-only: anti-join + Nullable, no hook) → challenge: prove via describe_table + run the exact paired strip anti-join → R2 CONFIRMED (Nullable confirmed; strip-leak `0`) → challenge: pin the blast-radius precondition (allow_nullable_key=1 is sole guard) → R3 CONFIRMED/high (precondition pinned: one-line config change away from biting).
- **C02** — R1 CONFIRMED (code asymmetry, all three) → challenge: quantify live unmerged boundary dups → R2 CONFIRMED (balancer `15,956`/uni `309`/swapr `11`, FINAL `0`) → challenge: show one key doubling at read-site → R3 CONFIRMED/high (window dups `15,956`; read-site probe `0` dups → orchestrator reframes as latent/near-miss, not proven live corruption).
- **C03** — R1 CONFIRMED (test absent in schema.yml) → challenge: prove warehouse grain unique → R2 CONFIRMED (FINAL dups `0`, pure coverage gap) → challenge: would the test false-fail (non-FINAL on RMT)? → R3 CONFIRMED/medium (recommend FINAL-qualified test).
- **C04** — R1 CONFIRMED (5 models test-less) → challenge: run grain-dup WITH FINAL each → R2 CONFIRMED (all `0`) → challenge: check non-FINAL split incremental vs table → R3 CONFIRMED/medium (curve/sdai non-FINAL `0`; cumsums dirty → split remediation note).
- **C05** — R1 CONFIRMED (append on batch vars, no runbook) → challenge: does refresh tooling compensate? → R2 CHANGED (gap_window_refresh.py drops partitions pre-backfill) → challenge: confirm drop covers all four UBO models at matching grain → R3 CHANGED/low (all four covered; compensation complete).
- **C06** — R1 CONFIRMED (table + RMT) → challenge: prove dedup redundant (DISTINCT) → R2 CONFIRMED (cosmetic engine) → R3 CONFIRMED/low (table live `69,726` rows).
- **C07** — R1 UNVERIFIABLE (claimed decode table not queryable) → challenge: orchestrator quantified `3,392`/`0` orphans → R2 RESOLVED (0 value dropped, holds for whitelisted pools) → R3 reverted to UNVERIFIABLE on code-only re-read → orchestrator REJECTED walk-back, settles RESOLVED/resolved.
- **C08** — R1 CONFIRMED (one-pass, aGnosDAI→aToken, no caveat) → challenge: bound materiality by aToken share → R2 CONFIRMED/high (magnitude still unquantified; generic container anti-join `0` because aGnosDAI not in known_containers) → challenge: measure by explicit aToken address → R3 CHANGED/medium (aGnosDAI terminal ubo `NULL`/0 of `70.49M` sDAI; ~0% overstatement; doc-only residual).
- **C09** — R1 CONFIRMED (argMax lookahead, undocumented) → challenge: quantify transferred-position share → R2 CONFIRMED (Uniswap `1.68%`/Swapr `14.05%` by tokenId) → challenge: value-weight Swapr → R3 CONFIRMED/medium (value-weighting not re-run; count-share bound uncontradicted, no escalation).
- **C10** — R1 CONFIRMED (Balancer single Vault, undocumented) → challenge: confirm consumer impact in venue view → R2 CHANGED (view is protocol-grained, no container column for any venue) → challenge: check no other mart re-exposes per-pool → R3 CHANGED/low (grep: no mart SELECTs int_ubo_claims_*; intermediate-only).
- **C11** — R1 CHANGED (only api_ carries tier1; both absent from schema.yml) → challenge: restate scope — only venue_breakdown absent → R2 CONFIRMED-scope (coverage + top_holders documented) → challenge: read check_api_tags.py enforcement → R3 CHANGED/low (rule #4 would flag but allowlisted; single tracked exemption).
- **C12** — R1 RESOLVED (coverage `25`/venue `80`, fresh `2026-06-19`) → challenge: add content sanity check → R2 RESOLVED (pct percent-scale reconciles to ~100, as_of `2026-06-20`) → R3 RESOLVED/resolved (re-measured `80`/`25`, fresh to `2026-06-18` upstream).
- **C13** — R1 CONFIRMED (whitelist scoping, implicit caveat) → challenge: show a concrete unlisted-token raw-container example → R2 CONFIRMED (code-proven; no example queried, budget) → R3 CONFIRMED/low (UNION-ALL whitelist joins re-confirmed).
- **C14** — R1 CONFIRMED (Curve AMM absent, Vault only in ABI seeds) → challenge: grep the Curve LP/gauge seeds to scope precisely → R2 CONFIRMED (gauge ABI seeded; AMM absent everywhere) → R3 CONFIRMED/low (exact-hex grep: both absent from all seeds).
- **C15** — R1 CONFIRMED (claims `53,409,975`/resolved `54,177,969`/delta `+767,994`) → challenge: decompose delta = added − stripped → R2 CONFIRMED (delta reproduced; `7,785` across `990` groups) → R3 CONFIRMED/low (arithmetic identity is the fan-out surplus, not drift).
- **C16** — R1 CONFIRMED (`7,785`/`11`/sDAI-only/leak `0`) → challenge: re-run with grain-uniqueness check → R2 CONFIRMED (grain dups `0`, correct token-paired leak `0`) → R3 CONFIRMED/low (all four checks re-measured).
- **C17** — R1 RESOLVED (all 7 protocols max `2026-06-18`, gap `0`) → challenge: verify daily contiguity June 1-18 + resolved mart inherits → R2 RESOLVED (resolved mart all 7 at `2026-06-18`) → R3 RESOLVED/resolved (all 7 now `2026-06-20`, gap closed).
- **C18** — R1 CONFIRMED (V1 `406`/V2 `1,326`, gapless) → challenge: confirm single collapsed EURe series, no boundary double-count → R2 CONFIRMED (V1 `406`/V2 `1,350`, no overlap) → R3 CONFIRMED/low (cutover `19959`→`19960` exact).

## Refreshed recommendations

| priority | recommendation | affected models |
|---|---|---|
| P1 (KEEP) | Add FINAL to the regular (non-batch) `prev_balances` CTE read of `FROM {{ this }}` so it matches the batch path; the asymmetry will double the cumsum starting balance whenever the single read-site date has an unmerged ReplacingMergeTree dup. | `models/execution/ubo/intermediate/int_ubo_claims_balancer_v2_daily.sql` (L78), `int_ubo_claims_uniswap_v3_daily.sql` (L280), `int_ubo_claims_swapr_v3_daily.sql` (L273) |
| P1 (KEEP) | Add `SET join_use_nulls=1` pre_hook to the resolved-mart strip anti-join, OR cast `container_address` to non-Nullable and adjust the anti-join — current correctness rests solely on `allow_nullable_key=1` + no non-null cast (a one-line schema change away from silently double-counting second-level UBOs). | `models/execution/ubo/marts/fct_ubo_supply_claims_resolved_daily.sql` |
| P2 (KEEP) | Add `dbt_utils.unique_combination_of_columns` at the redistribution grain `(date,protocol,container_address,ubo_address,token_address)`, keyed against a FINAL subquery (or lookback where-clause) to avoid importing the C02 pre-FINAL false-positive. | `models/execution/ubo/marts/schema.yml` (`fct_ubo_supply_claims_resolved_daily`) |
| P2 (KEEP) | Add uniqueness tests to the five intermediate claims models: plain test for curve/sdai (table-materialized, non-FINAL clean); FINAL-qualified test for balancer/uniswap/swapr (cumsum boundary dups would false-fail a default test). | `models/execution/ubo/intermediate/schema.yml` (balancer/uniswap/swapr/curve/sdai) |
| P3 (KEEP) | Add the venue_breakdown (fct+api) entry to `tokens/marts/schema.yml` with a published typed column schema, then remove the `columns_missing`/`no_as_of_date` lines from `check_api_tags.allow` so the CI guard enforces rather than tolerates the gap. | `models/execution/tokens/marts/schema.yml`, `models/execution/tokens/marts/api_execution_tokens_ubo_venue_breakdown_latest.sql`, `scripts/checks/check_api_tags.allow` |
| P3 (KEEP) | Document the attribution simplifications in `intermediate/schema.yml` ubo_address descriptions: argMax current-owner lookahead (Swapr `14.05%` of tokenIds transferred post-mint); and surface the one-pass second-level ceiling caveat in the coverage view description. | `models/execution/ubo/intermediate/schema.yml`, `models/execution/tokens/marts/fct_execution_tokens_ubo_coverage_latest.sql` |
| P4 (KEEP) | Add an explicit consumer-facing scoping caveat (unlisted tokens surface as raw container top-holders, no UBO decomposition) to the mart description. | `models/execution/ubo/marts/fct_ubo_supply_claims_daily.sql` (schema.yml) |
| P4 (KEEP) | Switch `fct_ubo_known_containers_daily` engine to plain `MergeTree()` (table + SELECT DISTINCT means the RMT dedup is never exercised — cosmetic/misleading). | `models/execution/ubo/marts/fct_ubo_known_containers_daily.sql` |
| P4 (KEEP) | Seed the Balancer Vault `0xba12...f2c8` and Curve 3pool AMM `0x7f90...f353` into a deployment registry seed and cross-check in-model, to harden against silent wrong-deployment. | `models/execution/ubo/intermediate/int_ubo_claims_balancer_v2_daily.sql`, `int_ubo_claims_curve_daily.sql`, `seeds/` |
| P5 (KEEP, nice-to-have) | Add an in-model operator-runbook comment noting append-mode requires the gap_window_refresh DROP PARTITION pre-step (compensation now exists but is undocumented in the models). | three cumsum intermediates + `fct_ubo_supply_claims_resolved_daily.sql` |
| — (DROP) | C07 Swapr decrease-orphan drop — RESOLVED, 0 orphans / 0 value dropped. No action. | `int_ubo_claims_swapr_v3_daily.sql` |
| — (DROP) | C12 coverage/venue mart presence — RESOLVED, both built/populated/fresh. No action. | `fct_execution_tokens_ubo_coverage_latest.sql`, `fct_execution_tokens_ubo_venue_breakdown_latest.sql` |
| — (DROP) | C17 sDAI/Curve staleness gap — RESOLVED, all 7 protocols aligned to `2026-06-20`. No action. | `int_ubo_claims_sdai_daily.sql`, `int_ubo_claims_curve_daily.sql` |
| — (DROP) | C08 aGnosDAI overstatement — magnitude proven ~0%; only the documentation caveat remains (folded into P3). | `fct_ubo_supply_claims_resolved_daily.sql` |
