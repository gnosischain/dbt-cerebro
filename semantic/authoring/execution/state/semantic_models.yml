semantic_models:
- name: execution_state_full_size_daily
  model: ref('api_execution_state_full_size_daily')
  defaults:
    agg_time_dimension: date
  dimensions:
  - name: date
    type: time
    expr: date
    type_params:
      time_granularity: day
  measures:
  - name: execution_state_full_size_daily__value_value
    agg: sum
    expr: value
  config:
    meta:
      cerebro:
        owner: analytics_team
        quality_tier: candidate
        question_synonyms:
        - execution state full size daily
        grain: day
- name: fct_execution_state_full_size_daily
  model: ref('fct_execution_state_full_size_daily')
  defaults:
    agg_time_dimension: date
  dimensions:
  - name: date
    type: time
    expr: date
    type_params:
      time_granularity: day
  measures:
  - name: bytes_value
    agg: sum
    expr: bytes
  config:
    meta:
      cerebro:
        owner: analytics_team
        quality_tier: candidate
        question_synonyms:
        - execution state full size daily
        grain: day
- name: execution_state_size_full_diff_daily
  model: ref('int_execution_state_size_full_diff_daily')
  defaults:
    agg_time_dimension: date
  dimensions:
  - name: date
    type: time
    expr: date
    type_params:
      time_granularity: day
  measures:
  - name: bytes_diff_value
    agg: sum
    expr: bytes_diff
  config:
    meta:
      cerebro:
        owner: analytics_team
        quality_tier: candidate
        question_synonyms:
        - execution state size full diff daily
        grain: day
metrics:
- name: execution_state_full_size_daily__value_value
  label: Execution State Full Size Daily - Value
  description: execution state full size daily - value (sum). Auto-generated candidate metric; review and promote before relying
    on it.
  type: simple
  type_params:
    measure: execution_state_full_size_daily__value_value
  config:
    meta:
      cerebro:
        quality_tier: candidate
        owner: analytics_team
        grain: day
        allowed_dimensions:
        - date
        supported_time_grains:
        - day
        - week
        - month
        - quarter
        - year
        question_synonyms:
        - execution state full size daily value
        - value
- name: bytes_value
  label: Execution State Full Size Daily - Bytes
  description: execution state full size daily - bytes (sum). Auto-generated candidate metric; review and promote before relying
    on it.
  type: simple
  type_params:
    measure: bytes_value
  config:
    meta:
      cerebro:
        quality_tier: candidate
        owner: analytics_team
        grain: day
        allowed_dimensions:
        - date
        supported_time_grains:
        - day
        - week
        - month
        - quarter
        - year
        question_synonyms:
        - execution state full size daily bytes
        - bytes
- name: bytes_diff_value
  label: Execution State Size Full Diff Daily - Bytes Diff
  description: execution state size full diff daily - bytes diff (sum). Auto-generated candidate metric; review and promote
    before relying on it.
  type: simple
  type_params:
    measure: bytes_diff_value
  config:
    meta:
      cerebro:
        quality_tier: candidate
        owner: analytics_team
        grain: day
        allowed_dimensions:
        - date
        supported_time_grains:
        - day
        - week
        - month
        - quarter
        - year
        question_synonyms:
        - execution state size full diff daily bytes diff
        - bytes diff
